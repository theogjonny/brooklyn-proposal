<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Brooklyn ❤️</title>

<style>
body {
    margin:0;
    overflow:hidden;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg,#ff4d4d,#990000);
    color:white;
    text-align:center;
}

/* Start screen */
#startScreen {
    position:fixed;
    width:100%;
    height:100%;
    display:flex;
    justify-content:center;
    align-items:center;
    flex-direction:column;
    background:black;
}

#startBtn {
    padding:20px 40px;
    font-size:25px;
    background:red;
    border:none;
    border-radius:15px;
    color:white;
    cursor:pointer;
}

/* Main card */
.container {
    display:none;
    position: relative;
    top: 50%;
    transform: translateY(-50%);
    background:white;
    color:black;
    padding:40px;
    border-radius:20px;
    box-shadow:0 10px 30px rgba(0,0,0,0.4);
}

h1 { font-size:42px; }

button {
    padding:15px 30px;
    font-size:20px;
    margin:15px;
    border:none;
    border-radius:10px;
    cursor:pointer;
    position:relative;
}

.yes { background:#ff1a1a; color:white; }
.no {
    background:#333;
    color:white;
    position:absolute;
}

/* Victory */
#victory {
    display:none;
    position:fixed;
    width:100%;
    height:100%;
    background: linear-gradient(#ff0000,#660000);
    justify-content:center;
    align-items:center;
    flex-direction:column;
    font-size:50px;
}

/* Confetti */
.confetti {
    position: fixed;
    width:10px;
    height:10px;
    top:0;
    animation: fall linear infinite;
}

@keyframes fall {
    to { transform: translateY(100vh) rotate(360deg); }
}
</style>
</head>

<body>

<!-- Start Screen -->
<div id="startScreen">
    <h1>🎮 Connecting to Brooklyn's Lobby...</h1>
    <button id="startBtn" onclick="start()">ENTER GAME</button>
</div>

<!-- Main Question -->
<div class="container">
    <h1>Brooklyn 🐐❤️</h1>

    <p>
    You already know you're the Fortnite GOAT...<br><br>
    And honestly? I kinda miss being on your team.
    </p>

    <p>
    I won't lie...<br>
    I’d drop anywhere on the map… as long as it’s with you 😏
    </p>

    <h2>So… will you be my girlfriend again? 🌷</h2>

    <button class="yes" onclick="yes()">YES</button>
    <button class="no" id="noBtn">NO</button>
</div>

<!-- Victory Screen -->
<div id="victory">
    <div>🏆 VICTORY ROYALE 🏆</div>
    <p>Brooklyn said YES ❤️</p>
</div>

<script>

// Start transition
function start(){
    document.getElementById("startScreen").style.display="none";
    document.querySelector(".container").style.display="inline-block";
}

// Moving NO button
const noBtn = document.getElementById("noBtn");

noBtn.addEventListener("mouseover", () => {
    const x = Math.random() * (window.innerWidth - 120);
    const y = Math.random() * (window.innerHeight - 60);

    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";
});

// YES button
function yes(){
    document.querySelector(".container").style.display = "none";
    document.getElementById("victory").style.display = "flex";

    // Confetti
    for(let i=0;i<120;i++){
        let conf = document.createElement("div");
        conf.classList.add("confetti");
        conf.style.left = Math.random()*100+"vw";
        conf.style.background = `hsl(${Math.random()*360},100%,50%)`;
        conf.style.animationDuration = (Math.random()*2+2)+"s";
        document.body.appendChild(conf);
    }
}

// Tulip petals
setInterval(() => {
    let petal = document.createElement("div");
    petal.innerHTML = "🌷";
    petal.style.position = "fixed";
    petal.style.left = Math.random()*100 + "vw";
    petal.style.top = "-50px";
    petal.style.fontSize = "26px";
    petal.style.animation = "fall 5s linear";

    document.body.appendChild(petal);

    setTimeout(()=>petal.remove(),5000);
}, 250);

</script>

</body>
</html>